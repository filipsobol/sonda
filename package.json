{
  "name": "pitbull-repo",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint --fix packages/*/src",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.1.0",
    "esbuild": "^0.23.0",
    "oxlint": "^0.7.0",
    "typescript": "^5.5.4"
  },
  "workspaces": [
    "packages/*"
  ]
}
